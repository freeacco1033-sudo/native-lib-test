name: Build Native Lib
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build .so
        run: bash build.sh
      - uses: actions/upload-artifact@v4
        with:
          name: native-libs
          path: libs/
